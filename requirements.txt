# artwall runtime dependencies
# Install with: pip install -r requirements.txt
# Or with the --break-system-packages flag on Ubuntu/Debian:
#   pip install -r requirements.txt --break-system-packages

requests>=2.28.0
Pillow>=9.0.0
